# Database
DATABASE_URL="file:./dev.db"

# Application
NODE_ENV="development"

# App URL (Required for OAuth callbacks)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Encryption Key (Change this in production!)
# Generate a secure key: openssl rand -hex 32
ENCRYPTION_KEY="dev-key-change-in-production-32b"

# For Cloud Deployment (Optional)
# DATABASE_URL="postgresql://user:password@host:5432/database?schema=public"

# Vercel Blob Storage (Required for PDF generation on Vercel)
# Get this from: https://vercel.com/dashboard/stores
# BLOB_READ_WRITE_TOKEN="vercel_blob_rw_..."
